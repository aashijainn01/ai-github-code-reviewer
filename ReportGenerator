public class ReportGenerator {

    public String generateReport() {
        String report = "";

        for (int i = 0; i < 1000; i++) {
            report += i;
        }

        return report;
    }
}
